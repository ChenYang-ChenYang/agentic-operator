{
  "name": "@agentic/ontology-compiler",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic compiler from an allmetaOntology domain export to an Agentic Operator tenant manifest (design G1).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./cli": "./src/cli.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "@agentic/runtime": "workspace:*",
    "@types/node": "^26.1.1",
    "typescript": "^6.0.3",
    "vitest": "^3.0.5"
  }
}
